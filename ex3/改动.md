# 2026-06-12 实验推进改动汇总

本文汇总从 `现阶段介绍.md` 接手后继续推进实验所做的改动、运行的验证、形成的结论和最终可复现实验入口。下方原有内容仍保留，作为更早阶段相对旧实验报告的变更说明。

---

## ★ 2026-06-12（最新）refine2 高速演示线：从提速扫描到满油门覆盖

> 目标变更：本线**不再追求 30s 生存或整圈**。整圈展示已定为 `cth_v1_gtrack1_refine3_balanced2_cpu/best`（`best_lap_time=58.27`，跑得完）；本线只为做一版 **refine2 的高速演示**，不管出圈/生存。
> 起点 checkpoint：`checkpoints/cth_v1_gtrack1_refine2/best_actormodel.pth`。
> 相关计划文档：[高速展示提速方案.md](/home/ccczorange/RL-learning/ex3/高速展示提速方案.md)、[圈速提升方案v2.md](/home/ccczorange/RL-learning/ex3/圈速提升方案v2.md)、[refine2高速演示修改计划.md](/home/ccczorange/RL-learning/ex3/refine2高速演示修改计划.md)。

### A.0 起点事实
- `runs/cth_v1_gtrack1_refine2/eval/episode_summary.csv`：`average_speed=152.045799`、`curLapTime=26.158s`、`distFromStart≈992m`、`out_of_track`、`max_abs_trackPos=1.44936`。
- eval 完全确定性（多行逐字节相同，seed=42），同一组参数每次结果一致，故每组只需 1 局正式（+1 局 warmup 隔离首局分叉）。
- refine2 当时未存 run config，152 是在 **guard 默认参数**下跑出来的。

### A.1 第一轮（高速展示提速方案）：guard 限速旋钮扫描 —— 提不动
按 [高速展示提速方案.md](/home/ccczorange/RL-learning/ex3/高速展示提速方案.md) 扫 `TORCS_CTH_PREVIEW_TARGET_SPEED_BASE`(176→230)、`TARGET_SPEED_MIN`、`MIN_SPEED` 等。

- 代表 run `refine2_fast_ms210_b5aux_0612a`：`avg=152.045799`、`dist=992.08`、**`guard_steps_ratio=0.0000`**。
- 所有提速档（`refine2_fast_ms140~ms220`、`b6~b9`）结果**逐字节相同** 152.045799 / 26.158s / 992m。

**关键诊断**：最终参数里 `TORCS_CTH_PREVIEW_MIN_SPEED=210` 表示「车速低于 210km/h 时 guard 完全不介入」，而车从没超过 152 → guard 整局零介入。**152 不是被限速压出来的，是 actor 网络自己的输出上限；那个速度封顶夹子从没夹住过车**，所以怎么调都是 152。

### A.2 第二轮（圈速提升方案v2）：反向激活 guard 在弯前减速 —— 方向对一半
按 [圈速提升方案v2.md](/home/ccczorange/RL-learning/ex3/圈速提升方案v2.md)，把 `MIN_SPEED` 降到 80~100 **重新激活 guard**，让它在 992m 弯前主动减速，用弯前减速换过弯。

| run | dist(m) | curLap(s) | avg | term | guard_ratio |
|---|---|---|---|---|---|
| `refine2_corner_g0`（30s 截断） | 875.63 | 30.198 | 116.46 | success_30s | 0.3758 |
| `refine2_corner_g0_lapcheck`（最远） | **1800.89** | 58.016 | 115.07 | out_of_track | 0.3472 |
| `refine2_corner_r3_lapcheck`（较快） | 1798.43 | 54.216 | 123.67 | out_of_track | 0.2342 |

- guard 成功激活（ratio>0），出圈点从 992m（整圈 2545m 的 39%）推到 **~1800m（70.7%）**，方向有效。
- 但 **5 个差异极大的策略**（avg 99.8~124、guard 0.23~0.44、圈时 54~66s）**全部死在 1720~1801m 的 80m 窗口内**。这是「结构性过弯失败」的指纹：不管快慢、刹多刹少都死在同一个弯 → refine2 actor 本身不会处理那个弯，纯 guard 只能延后失败、救不了转向。更刹反而更糟（`g2` guard 最重 0.436 却跑最短 1720m）。
- 结论：按方案 §5 回退判断——eval 调参不能完成整圈。**整圈定 refine3，高速线回到 refine2。**

### A.3 代码层确证：环境变量提速已到底
逐行核对 refine2(`cth_v1`) eval 时纵向 accel/brake 经过的 3 层，全部失效：

| 层 | 代码位置 | cth_v1 状态 | 结论 |
|---|---|---|---|
| lap_safe 安全壳 accel-cap | `gym_torcs.py:1705-1718+` | `resolve_safety_shell_enabled()` 对 cth 返回 `False`（仅 lap_safe/tal 才 True）→ 1705 行早退 | 整层跳过 |
| stability assist `accel_cap=0.68` | `gym_torcs.py:1648-1655` | `build_teacher_policy()` 仅 tal 返回非 None；cth → `teacher_policy=None` → 1650 行早退 | 死代码，从不运行 |
| preview guard 速度封顶 | `gym_torcs.py:1489+` | A.1 实测封顶 176→230 结果逐字节相同；MIN_SPEED=210 时 ratio=0.0 | 不夹速度 |

**铁结论：refine2 eval 的纵向命令 = actor 网络原始输出，无任何环境变量可改。纯调参提速到此结束。**

### A.4 代码改动：demo 满油门覆盖开关
按 [refine2高速演示修改计划.md](/home/ccczorange/RL-learning/ex3/refine2高速演示修改计划.md) 的路 B，在 `DDPG_Torcs_PyTorch/gym_torcs.py` 新增（默认关闭，不影响任何旧路径 / refine3 / 历史复现）：

- **开关解析（`gym_torcs.py:131` 附近）**：
  - `TORCS_DEMO_FULL_THROTTLE`（默认 `0`=关）
  - `TORCS_DEMO_ACCEL_FLOOR`（默认 `1.0`）
  - `TORCS_DEMO_BRAKE_CEIL`（默认 `0.0`）
- **注入点（`gym_torcs.py:365`，`step()` 内、safety shell 之后）**：
  ```python
  safe_action = self.apply_action_safety_shell(client.S.d, requested_action)
  if self.demo_full_throttle:
      safe_action["accel"] = max(safe_action.get("accel", 0.0), self.demo_accel_floor)
      safe_action["brake"] = min(safe_action.get("brake", 0.0), self.demo_brake_ceil)
  ```
- **确认覆盖真的到达引擎**：cth 是 `throttle=True`，走 `gym_torcs.py:392-393` 的 `action_torcs["accel"]=safe_action["accel"]` / `["brake"]=safe_action["brake"]` → `respond_to_server()` 发车。不是空操作。
- 只动 accel/brake，**保留 actor 的 steer**；纯执行期动作覆盖，不碰网络权重、不写 checkpoint、不训练。
- `py_compile` 通过。

### A.5 验证结果（S1~S4）
| 步 | run | avg_speed | curLap | dist | 说明 |
|---|---|---|---|---|---|
| S1 现状峰值 | `refine2_demo_s1_visible_peak_0612a` | 152.045799（全程均速） | 26.158 | 992 | **直道瞬时峰值 max_speedX=201.964 km/h @ 25.350s** |
| S2 默认关回归 | `refine2_demo_s2_defaultoff_0612a` | 152.045799 | 26.158 | 992.08 | 旧路径未被破坏 |
| S3 满油门 headless | `refine2_demo_s3_fullthrottle_0612a` | 152.045799 | 26.158 | 992.08 | `applied_accel=1.0`/`applied_brake=0.0`，与默认**逐字节相同** |
| S4 满油门 visible | `refine2_demo_s4_display1_fullthrottle_0612b` | 152.045799 | 26.158 | 992.08 | 与 headless 一致 |

**因果证明**：S3 强制满油门/零刹车且确实下发引擎，结果却与默认逐字节相同 → refine2 actor **本来每一步就在输出满油门/零刹车**。覆盖开关生效但无余量可挖，车是**引擎/挡位限速**，不是油门限速。

### A.6 本线结论与交付
- **refine2 的物理天花板 = 全程均速 152.045799 km/h / 直道峰值 ~201.964 km/h（@25.35s，撞 992m 弯前）。** 再快需重训 actor（不在本线范围）。
- **演示口径**：高速 demo 应展示**直道峰值 ~202 km/h**，不要用含起步加速段的全程均速 152 作为主数字。高速片段 = 992m 弯之前那段直道。
- **最终交付（双版本）**：整圈展示 = `refine3_balanced2_cpu/best`（58.27s 完整圈）；高速展示 = `refine2/best`（峰值 ~202km/h 直道）。两者并列、各自标注定位。
- **遗留待补**：`201.964` 是交付里最重要的数字，但目前**未落盘**（`refine2_demo_s1_visible_peak_0612a/eval/` 无 CSV，峰值来自 stdout 临时读取）。后续应做成可复现证据（每步 speedX 落盘取 max，或 visible 峰值瞬间截 HUD 图）后再写进报告。

---

## 0. 下阶段计划执行结果：guard 天花板扫描 + 纯蒸馏分支

### 0.1 guard 天花板扫描

已按 [下阶段计划.md](/home/ccczorange/RL-learning/ex3/下阶段计划.md) 的决策门执行：

```bash
cd /home/ccczorange/RL-learning/ex3/DDPG_Torcs_PyTorch
./sweep_guard_ceiling.sh
/home/ccczorange/anaconda3/envs/ddpg/bin/python parse_ceiling.py
```

补跑了首次因 PyTorch 内部时钟断言中断的 `min126` 单组。最终解析结果显示存在更快安全包络，属于计划中的“情况甲”：

- `clear55`：`5/5 success_30s`，`avg_speed=131.5778`，但 `mean_abs_steer_delta=0.1147`
- `base182`：`5/5 success_30s`，`avg_speed=130.9779`，`mean_abs_steer_delta=0.0856`
- `a2_ref`：`5/5 success_30s`，`avg_speed=130.3633`
- `min126` / `combofast` 等更快配置均失败（`out_of_track`）

结论：确有 guard 参数层面的提速空间，但最快 `clear55` 的平顺性代价较大，`base182` 是更稳的蒸馏候选。

### 0.2 代码改动

更新 [DDPG_Torcs_PyTorch/test.py](/home/ccczorange/RL-learning/ex3/DDPG_Torcs_PyTorch/test.py)：

- 新增 `TORCS_TD3BC_MODE=bc_only`：纯 BC 蒸馏，只更新 actor，不更新 critic，不使用 Q 项
- 新增 `TORCS_BC_OFFLINE_UPDATES`：先 prefill teacher buffer，再离线做 actor-only BC 更新，避免 episode 中途改策略
- 新增 `TORCS_TD3BC_MODE=leash`、`TORCS_BC_WEIGHT`：支持计划中的单位归一化 RL 项 + 显式 BC 缰绳
- 新增 `TORCS_TD3_CRITIC2_INDEPENDENT=1`：支持 critic2 独立初始化
- 新增 `td3_diag.csv`：记录 `rl_term`、`bc_loss`、`weighted_bc_loss`、`q_pi.mean()`、`|q_pi|.mean()`；`bc_only` 下 Q 字段为空

已通过：

- `python -m py_compile test.py ActorNetwork.py CriticNetwork.py ReplayBuffer.py`
- 不连 TORCS 的张量级自测：`bc_only` / `leash` 均可反传，独立 critic2 与 critic 不同
- 回退自检：`cth_v1_eval_post_codechange_selfcheck` 复现 `5/5 success_30s`、`avg_speed=130.363325`
- 可视化回退自检：`cth_v1_eval_visible_post_codechange_selfcheck` 复现 `5/5 success_30s`、`avg_speed=130.363325`

### 0.3 纯蒸馏候选结果

按“情况甲 -> 纯蒸馏 guard”分支执行，但没有得到合格 student checkpoint：

| run | 结果 | 结论 |
| --- | --- | --- |
| `cth_v1_bc_distill_clear55_s0` | online 第 1 局即 `out_of_track` | `clear55` 太贴安全边界，在线更新不可用 |
| `cth_v1_bc_distill_base182_s0` | interval eval `0/5`，全部 `no_progress` | online BC 会把 actor 推离稳定区 |
| `cth_v1_eval_bc_distill_clear55_offline_u20` | `0/5`，全部 `out_of_track` | 最快 guard 不适合当前 student 蒸馏 |
| `cth_v1_eval_bc_distill_base182_offline_u20_lr1e6` | `0/5`，全部 `no_progress` | 20 次离线更新仍过强 |
| `cth_v1_eval_bc_distill_base182_offline_u1_lr1e7` | `5/5`，但 `avg_speed=130.315050` | 稳定但低于 demo `130.363325`，不合格 |
| `cth_v1_eval_bc_distill_base182_offline_u1_lr1e8` | `0/5`，全部 `collision` | 不合格 |

结论：本轮纯蒸馏没有拿到同时满足 `5/5 success_30s` 且 `avg_speed > 130.363325` 的学习策略。按计划纪律，不能把蒸馏/TD3+BC 写成最终展示路线；最终交付仍回退并锁定已验证 demo：`cth_v1_gtrack1_refine3_balanced2_cpu/best` + a2 guard + `run_final_30s_eval.sh`。

## 1. 本轮实际新增和修改的文件

### 1.1 最终验收脚本

新增：

- [DDPG_Torcs_PyTorch/run_final_30s_eval.sh](/home/ccczorange/RL-learning/ex3/DDPG_Torcs_PyTorch/run_final_30s_eval.sh)

作用：

- 固化最终 30 秒验收入口
- 支持 `headless` 和 `visible` 两种模式
- 统一使用同一个 checkpoint：
  - `cth_v1_gtrack1_refine3_balanced2_cpu/best`
- 统一使用当前最终验收配置：
  - `TORCS_STANDARDIZE_FIRST_RESET=0`
  - `TORCS_EVAL_WARMUP_EPISODES=1`
  - `TORCS_RELAUNCH_EVERY=0`
  - `TORCS_CHECKPOINT_SCORE_MODE=30s_pace_first`
  - a2 guard 参数组
- 已做 `bash -n` 语法检查
- 已实际跑通：
  - `./run_final_30s_eval.sh headless`
  - `./run_final_30s_eval.sh visible`

### 1.2 C2 TD3+BC 实验记录

新增：

- [DDPG_Torcs_PyTorch/runs/cth_v1_td3bc_s0c2/protocol.md](/home/ccczorange/RL-learning/ex3/DDPG_Torcs_PyTorch/runs/cth_v1_td3bc_s0c2/protocol.md)
- [DDPG_Torcs_PyTorch/runs/cth_v1_td3bc_s0c2/analysis.md](/home/ccczorange/RL-learning/ex3/DDPG_Torcs_PyTorch/runs/cth_v1_td3bc_s0c2/analysis.md)

生成实验输出：

- [DDPG_Torcs_PyTorch/runs/cth_v1_td3bc_s0c2/train/episode_summary.csv](/home/ccczorange/RL-learning/ex3/DDPG_Torcs_PyTorch/runs/cth_v1_td3bc_s0c2/train/episode_summary.csv)
- [DDPG_Torcs_PyTorch/runs/cth_v1_td3bc_s0c2/interval_eval_summary.csv](/home/ccczorange/RL-learning/ex3/DDPG_Torcs_PyTorch/runs/cth_v1_td3bc_s0c2/interval_eval_summary.csv)
- [DDPG_Torcs_PyTorch/checkpoints/cth_v1_td3bc_s0c2/best_checkpoint_metrics.csv](/home/ccczorange/RL-learning/ex3/DDPG_Torcs_PyTorch/checkpoints/cth_v1_td3bc_s0c2/best_checkpoint_metrics.csv)

### 1.3 最终验收 run 输出

新增最终脚本复核输出：

- [DDPG_Torcs_PyTorch/runs/cth_v1_eval_headless_final30s_a2_warm1/eval/episode_summary.csv](/home/ccczorange/RL-learning/ex3/DDPG_Torcs_PyTorch/runs/cth_v1_eval_headless_final30s_a2_warm1/eval/episode_summary.csv)
- [DDPG_Torcs_PyTorch/runs/cth_v1_eval_visible_final30s_a2_warm1/eval/episode_summary.csv](/home/ccczorange/RL-learning/ex3/DDPG_Torcs_PyTorch/runs/cth_v1_eval_visible_final30s_a2_warm1/eval/episode_summary.csv)

同时保留一份可视化预复核输出：

- [DDPG_Torcs_PyTorch/runs/cth_v1_eval_visible_speedprobe_a2_warm1/eval/episode_summary.csv](/home/ccczorange/RL-learning/ex3/DDPG_Torcs_PyTorch/runs/cth_v1_eval_visible_speedprobe_a2_warm1/eval/episode_summary.csv)

### 1.4 阶段文档更新

更新：

- [现阶段介绍.md](/home/ccczorange/RL-learning/ex3/现阶段介绍.md)

主要更新内容：

- 将 `cth_v1_td3bc_s0c2` 从“待验证 C2 方案”改为“已验证失败”
- 增加 C2 训练和 interval eval 结果
- 增加最终 headless / visible 复核结果
- 将最终推荐路线更新为：
  - 稳定 DDPG baseline checkpoint
  - `1` 局 eval warmup
  - a2 guard 参数
  - 不继续沿 TD3+BC 失败分支续训
- 将下一步从“继续训练”调整为“固化验收命令和整理报告材料”

## 2. 本轮运行的关键实验

### 2.1 TD3+BC C2 强锚定验证

实验 run：

- `cth_v1_td3bc_s0c2`

实验目的：

- 验证降低 TD3+BC 中 RL/Q 项强度后，是否能阻止 Stage C 中出现的 critic / actor 漂移

关键配置：

- `TORCS_TD3=1`
- `TORCS_BOOTSTRAP_SOURCE=cth_v1_gtrack1_refine3_balanced2_cpu`
- `TORCS_BC_ALPHA_START=0.1`
- `TORCS_BC_ALPHA_END=0.1`
- `TORCS_LRA=1e-5`
- `TORCS_LRC=1e-4`
- `TORCS_STEER_SIGMA=0.03`
- `TORCS_ACCEL_SIGMA=0.01`
- `TORCS_BRAKE_SIGMA=0.01`
- `TORCS_RELAUNCH_EVERY=0`
- `TORCS_EVAL_WARMUP_EPISODES=1`

结果：

- 训练只跑到 ep29 interval eval 后提前停止
- 训练 30 局结果：
  - `8/30 success_30s`
  - `15/30 out_of_track`
  - `7/30 no_progress`
  - `average_speed ≈ 100.49`
- ep14 interval eval：
  - `5/5 success_30s`
  - `average_speed = 127.562428`
  - 速度低于 `avg_speed >= 129` 判据
- ep29 interval eval：
  - `0/5 success_30s`
  - `5/5 out_of_track`
  - `average_speed = 106.785870`

结论：

- C2 已失败
- `alpha=0.1`、低学习率、低噪声仍不足以阻止 TD3+BC 漂移
- `cth_v1_td3bc_s0c2` 不能作为新 bootstrap
- 不应进入 Stage D / Final visible eval
- 若继续 TD3+BC，必须先加 actor-loss 诊断日志，直接观察：
  - `lambda`
  - `-lambda * q_pi.mean()`
  - `bc_loss`

### 2.2 可视化 a2 + warmup 复核

实验 run：

- `cth_v1_eval_visible_speedprobe_a2_warm1`

实验目的：

- 复核 headless 最优 a2 guard 配置在 `TORCS_VISIBLE=1` 下是否也能通过 30 秒验收
- 检查旧 visible baseline 首局 `11.258s out_of_track` 是否能被 warmup 隔离

结果：

- `5/5 success_30s`
- `0 out_of_track`
- `0 collision`
- `average_speed = 130.363325`
- `guard_steps_ratio = 0.154362`
- `mean_abs_steer_delta = 0.080570`

结论：

- 可视化模式下，`1` 局 warmup + a2 guard 能稳定通过正式 5 局
- 旧 visible 首局失败不再影响正式统计

### 2.3 最终脚本 headless / visible 复核

脚本：

- [DDPG_Torcs_PyTorch/run_final_30s_eval.sh](/home/ccczorange/RL-learning/ex3/DDPG_Torcs_PyTorch/run_final_30s_eval.sh)

运行：

```bash
cd /home/ccczorange/RL-learning/ex3/DDPG_Torcs_PyTorch
./run_final_30s_eval.sh headless
./run_final_30s_eval.sh visible
```

headless final：

- run：`cth_v1_eval_headless_final30s_a2_warm1`
- `5/5 success_30s`
- `0 out_of_track`
- `0 collision`
- `average_speed = 130.363325`
- `mean_abs_steer_delta = 0.080570`
- `guard_steps_ratio = 0.154362`

visible final：

- run：`cth_v1_eval_visible_final30s_a2_warm1`
- `5/5 success_30s`
- `0 out_of_track`
- `0 collision`
- `average_speed = 130.363325`
- `mean_abs_steer_delta = 0.080570`
- `guard_steps_ratio = 0.154362`

结论：

- 最终脚本的 headless 和 visible 入口都已验证通过
- 当前最终展示候选已经形成

## 3. 当前最终推荐配置

最终 checkpoint：

- `cth_v1_gtrack1_refine3_balanced2_cpu/best`

最终运行配置：

- `TORCS_PROFILE=cth_v1`
- `TORCS_TRAIN=0`
- `TORCS_CHECKPOINT_RUN_NAME=cth_v1_gtrack1_refine3_balanced2_cpu`
- `TORCS_EVAL_USE_BEST=1`
- `TORCS_CHECKPOINT_SCORE_MODE=30s_pace_first`
- `TORCS_STANDARDIZE_FIRST_RESET=0`
- `TORCS_EVAL_WARMUP_EPISODES=1`
- `TORCS_RELAUNCH_EVERY=0`
- `TORCS_EPISODES=5`
- `TORCS_MAX_STEPS=3000`

最终 a2 guard 参数：

- `TORCS_CTH_PREVIEW_MIN_SPEED=122`
- `TORCS_CTH_PREVIEW_CLEARANCE_START=65`
- `TORCS_CTH_PREVIEW_TARGET_SPEED_BASE=177`
- `TORCS_CTH_PREVIEW_TARGET_SPEED_MIN=106`
- `TORCS_CTH_PREVIEW_HEADING_START_DEG=7`

验收入口：

- headless：`./run_final_30s_eval.sh headless`
- visible：`./run_final_30s_eval.sh visible`

## 4. 当前明确不推荐继续使用的分支

不推荐继续续训或作为最终展示：

- `cth_v1_30s_speedft_a1r1`
  - 独立 probe 为 `0/5 success_30s`
  - `5/5 out_of_track`
- `cth_v1_td3bc_s0`
  - Stage C 后退化到 `0/5 success_30s`
- `cth_v1_td3bc_s0c2`
  - ep29 interval eval 退化到 `0/5 success_30s`
- `a3` guard
  - 虽然稳定，但速度下降、转向抖动升高
- `a2_brake20`
  - 虽然稳定，但速度和动作平顺性都弱于 a2

## 5. 本轮没有做的事情

- 没有继续从失败 checkpoint 做长训
- 没有把 TD3+BC 作为最终路线
- 没有删除已有历史实验输出
- 没有重置或回滚已有代码改动
- 没有把 `gym_torcs.py` / `test.py` 做新的大规模重构

## 6. 当前阶段结论

当前阶段已经从“继续寻找新训练分支”收口到“最终候选配置已经形成”：

- 最终策略仍使用稳定 DDPG checkpoint：
  - `cth_v1_gtrack1_refine3_balanced2_cpu/best`
- 最终验收依赖：
  - `TORCS_STANDARDIZE_FIRST_RESET=0`
  - `TORCS_EVAL_WARMUP_EPISODES=1`
  - a2 guard 参数组
- headless 和 visible 都已通过最终脚本复核：
  - `5/5 success_30s`
  - `0 out_of_track`
  - `0 collision`
  - `average_speed = 130.363325`

后续最合理的工作不是继续盲目训练，而是：

1. 整理最终报告材料
2. 把失败分支标注清楚，避免误用
3. 如需继续 TD3+BC，先加 actor-loss 诊断日志，再启动新的失败分支 probe

---

# 相比上一版实验报告的新增改动说明

本文用于对比：

- 旧版实验报告：`DDPG_Torcs_PyTorch/report_materials/04_report_ready_full_draft.md`
- 当前阶段说明：`现阶段介绍.md`

核心结论是：当前版本已经不再是“保持原始 baseline 奖励不变，只做 DDPG 超参数调优”的实验，而是切换到了 **CTH 奖励函数与 preview guard 联合优化** 的新阶段。实验目标、评价指标、代码结构和结论重点都发生了明显变化。

---

## 1. 实验主线发生了根本变化

### 旧版报告的主线

旧版报告的核心主线是：

- 以 `baseline` 为原始对照组；
- 在 **不修改 baseline reward** 的前提下，得到 `baseline_tuned`；
- 用 `g-track-2` 做零样本泛化验证；
- 用 `push1` 说明“高收益但高风险”的额外续训结果。

旧版报告在开头就明确写到：

- “在**不修改 baseline 奖励函数**的前提下调出更稳定的 `baseline_tuned`”
- “在新赛道 `g-track-2` 上进行零样本验证”
- “保留一次额外续训得到的 `push1` 结果”

因此旧版报告本质上还是一份：

- `baseline -> baseline_tuned -> push1`
- “稳定性优先于极限奖励”
- “单赛道训练 + 跨赛道零样本验证”

的 DDPG 基线调参报告。

### 当前阶段的新主线

当前阶段已经明确切换到：

- 主线是 `CTH`，不是 `TAL`
- 主赛道固定为 `g-track-1`
- 目标优先级从“尽量稳定不出界”升级为“**先稳定完整一圈，再继续压圈速**”

也就是说，当前工作不再是旧报告里的“调出一个更稳定的 baseline_tuned”，而是进入了新的问题设定：

1. 修改奖励函数
2. 引入 preview guard
3. 重新定义最优 checkpoint 的评价标准
4. 从“能在赛道内维持一段时间”推进到“稳定单圈完成”

这属于实验目标层面的升级，而不是简单的参数微调续作。

---

## 2. 奖励函数设定发生了变化

### 旧版报告

旧版报告强调最终模型仍使用原始 `baseline reward`，即：

$$
r_t = v_t \cos(\alpha_t) - |v_t \sin(\alpha_t)| - v_t |p_t|
$$

其逻辑是：

- 奖励沿赛道方向前进
- 惩罚横向漂移
- 惩罚偏离赛道中心

这套奖励函数没有显式鼓励“完整圈数”或“圈速”，更偏向基础赛道保持。

### 当前阶段

当前阶段已经引入新的 `CTH reward` 分支，代码实现位于：

- `DDPG_Torcs_PyTorch/gym_torcs.py:985`

新增内容包括：

- 速度奖励 `speed_reward`
- 前进距离奖励 `progress_reward`
- 赛道中心偏移惩罚 `center_penalty`
- 平滑惩罚 `smooth_penalty`
- 赛道边缘惩罚 `edge_penalty`
- 高速贴边惩罚 `speed_edge_penalty`
- 外甩惩罚 `outward_penalty`
- 低推进惩罚 `low_progress_penalty`
- 航向惩罚 `heading_penalty`
- 完成一圈后的反向惩罚 `reverse_penalty`
- 单圈奖励 `lap_bonus`

特别重要的是，当前 CTH 奖励已经引入了“**post-lap**”思路，即：

- 一圈完成后，对后段航向失控更敏感
- 一圈完成后，对低推进和倒车进行额外惩罚

这和旧版报告“只强调主赛道稳定、不改 baseline reward”的实验范式有本质区别。

### 当前额外接入但尚未跑完的 reward 参数

`现阶段介绍.md` 明确提到，已经接入但还未完全做成长训练验证的参数包括：

- `TORCS_CTH_PROGRESS_SCALE`
- `TORCS_CTH_HEADING_PENALTY_START`
- `TORCS_CTH_HEADING_SPEED_SCALE`
- `TORCS_CTH_POST_LAP_HEADING_MULTIPLIER`
- `TORCS_CTH_POST_LAP_LOW_PROGRESS_SCALE`
- `TORCS_CTH_POST_LAP_REVERSE_SCALE`

这些参数都已经进入 `compute_cth_reward()`，说明当前版本相较旧报告新增了“后段圈后稳定性优化”的奖励设计。

---

## 3. 评价指标体系发生了升级

### 旧版报告的核心指标

旧版主要用以下指标评价模型：

- 平均总奖励
- 平均速度
- 平均步数
- 终止原因
- 是否 `out_of_track`

这套指标适合回答：

- 是否比 baseline 更稳定
- 是否更少冲出赛道
- 是否具备一定泛化能力

### 当前阶段新增的关键指标

当前阶段已经把评价重心升级到“单圈完成质量”，新增指标包括：

- `lap_count_sum`
- `best_lap_time`
- `distRaced_mean`
- `avg_damage_sum`
- `stall_like_count`

对应实现和记录逻辑位于：

- `DDPG_Torcs_PyTorch/gym_torcs.py:636-759`
- `DDPG_Torcs_PyTorch/test.py:505-548`
- `DDPG_Torcs_PyTorch/test.py:1039-1117`

这意味着当前实验不再只看“没出界多久”，而是开始系统关注：

- 是否真的跑完整圈
- 跑完一圈用了多久
- 是否碰撞
- 是否后期失控磨停

这是从“生存导向指标”向“竞速导向指标”的明显升级。

---

## 4. 最优结果的定义发生了变化

### 旧版最优结果

旧版报告中，最终最优模型是：

- `baseline_tuned`

其核心结论是：

- 比 `baseline` 稳定得多
- 在 `g-track-1` 上不再出界
- 在 `g-track-2` 上有一定零样本泛化能力
- 比 `push1` 更适合作为“可交付”模型

### 当前阶段最优结果

当前阶段最强 run 已变为：

- `cth_v1_gtrack1_refine3_balanced2_cpu`

其 best checkpoint 指标为：

- `best_lap_time = 58.27 s`
- `average_speed_mean = 124.348171`
- `distRaced_mean = 2545.06`
- `lap_count_sum = 3`

最终评测结果为：

- `5/5` 稳定完成 `1 lap`
- 终止原因为 `no_progress`

这些数据来自：

- `DDPG_Torcs_PyTorch/checkpoints/cth_v1_gtrack1_refine3_balanced2_cpu/best_checkpoint_metrics.csv`
- `DDPG_Torcs_PyTorch/runs/cth_v1_gtrack1_refine3_balanced2_cpu/eval/episode_summary.csv`

与上一阶段最强基线 `cth_v1_gtrack1_refine2` 相比：

- `average_speed_mean: 152.045799 -> 124.348171`
- `distRaced_mean: 1092.08 -> 2545.06`
- `lap_count_sum: 0 -> 3`
- 从 `5/5 out_of_track` 变成 `5/5 稳定完成 1 lap`

新的最优性不再体现为“速度更高”，而体现为：

- 虽然平均速度下降
- 但成功从“高速但跑不完整圈”进化为“稳定完成单圈”

这是当前版本相对旧报告最重要的实验结论变化。

---

## 5. 新增了 preview guard 这一层安全控制机制

旧版报告里并没有系统性的 preview guard 设计，主要还是依赖策略网络本身和奖励函数进行学习。

当前阶段新增了：

- `compute_track_preview()`：`DDPG_Torcs_PyTorch/gym_torcs.py:1164`
- `apply_cth_preview_guard()`：`DDPG_Torcs_PyTorch/gym_torcs.py:1186`

其作用是：

- 利用前方赛道传感器计算预瞄方向和净空
- 在高速或急弯风险场景下，提前调整油门、刹车和转向
- 在策略动作外层再套一层“保护壳”

这是当前版本和旧版最大的结构性改动之一，因为它意味着：

- 旧版是“纯 RL 策略控制为主”
- 新版是“RL 策略 + 预瞄安全保护”的混合控制思路

### guard 扫描带来的新增实验结论

当前阶段已经新增专门扫描脚本：

- `DDPG_Torcs_PyTorch/scan_cth_guard.py`

并得到新的经验结论：

- `balanced_2` 是当前最好的稳定性/速度平衡点
- 关键参数为：
  - `TORCS_CTH_PREVIEW_MIN_SPEED=120`
  - `TORCS_CTH_PREVIEW_CLEARANCE_START=66`
  - `TORCS_CTH_PREVIEW_TARGET_SPEED_BASE=176`
  - `TORCS_CTH_PREVIEW_TARGET_SPEED_MIN=105`
  - `TORCS_CTH_PREVIEW_HEADING_START_DEG=7`

这部分在旧版报告中完全不存在，属于新增的实验模块。

---

## 6. checkpoint 选择逻辑发生了变化

旧版报告默认还是“稳定性优先”的模型选择逻辑，强调不出界、步数更长、泛化更稳。

当前阶段在 `test.py` 中新增了：

- `CHECKPOINT_SCORE_MODE`
- `pace_first`
- `stability_first`

代码位置：

- `DDPG_Torcs_PyTorch/test.py:327-337`

新的规则是：

- 对 `cth*` 主线，默认采用 `pace_first`
- 对旧非 CTH 逻辑，保留 `stability_first`

这说明当前最优 checkpoint 的定义已经改变：

- 旧版更强调“不坏”
- 新版开始在“完成单圈”的基础上考虑“更快”

这是实验筛选标准层面的新增改动。

---

## 7. 训练流程与工程实现有新增修复

相较旧报告，当前阶段在训练工程化方面新增了几类重要修改。

### 7.1 TORCS 启动与显示环境修复

在 `gym_torcs.py` 中新增了更完整的 TORCS 启动流程：

- `ensure_display()`
- `start_torcs()`
- 自动管理 `Xvfb`
- 使用 `torcs-local/share/games/torcs` 作为工作目录

代码关键位置：

- `DDPG_Torcs_PyTorch/gym_torcs.py:531`
- `DDPG_Torcs_PyTorch/gym_torcs.py:586`

这意味着当前版本比旧版更能稳定解决：

- 无图形环境下的 TORCS 启动问题
- 本地依赖路径问题
- 反复等待服务器的问题

### 7.2 训练节奏控制新增

`test.py` 里新增了：

- `TORCS_LEARN_START`
- `TORCS_UPDATE_EVERY`
- `TORCS_RELAUNCH_EVERY=0` 的除零修复

代码位置：

- `DDPG_Torcs_PyTorch/test.py:453`
- `DDPG_Torcs_PyTorch/test.py:468-469`
- `DDPG_Torcs_PyTorch/test.py:833`
- `DDPG_Torcs_PyTorch/test.py:916`
- `DDPG_Torcs_PyTorch/test.py:925`

这些改动带来的意义是：

- 可以做更保守的 refine
- 可以延迟开始学习
- 可以稀疏参数更新
- 能更稳定地从已有 checkpoint 继续训练

这也是当前阶段相对旧报告新增的重要工程能力。

---

## 8. 赛道与泛化叙事发生了变化

### 旧版报告

旧版强调：

- 训练赛道是 `g-track-1`
- 零样本验证赛道是 `g-track-2`
- 重点之一是跨赛道泛化

### 当前阶段

当前阶段明确收缩主线：

- 训练/评测赛道统一回到 `g-track-1`
- 特别强调不要再混到 `g-track-2`
- 当前首要目标是把单圈稳定性做扎实

因此，当前版本的重心已经从：

- “跨赛道零样本泛化”

转为：

- “单赛道上先把单圈完整性和圈速做出来”

这也是实验叙事上的重要变化。并不是说泛化不重要，而是当前阶段把研究重点前移到了“主赛道稳定单圈”。

---

## 9. 新增了 CPU/GPU 差异这一工程发现

旧版报告里没有把 CPU/GPU 差异作为主要实验结论。

当前阶段新增的重要发现是：

- 同一 checkpoint
- 同一 guard 参数
- `CPU` 与 `GPU` 的轨迹表现会明显不同

现阶段总结给出的结论是：

- `CPU` 上的 `balanced_2` 可以稳定跑出长距离甚至完整一圈
- `GPU` 上会明显退化

这意味着当前 CTH 主线在工程上新增了一个非常重要的实验约束：

- 参数扫描优先用 `CPU`
- 保守续训优先用 `CPU`
- best checkpoint 固化优先用 `CPU`

这个结论对复现实验和答辩解释都很重要，属于旧报告中没有的新内容。

---

## 10. 对失败方向的认识更具体了

旧版报告虽然也分析了 `push1` 这种“高收益但高风险”的失败案例，但失败解释仍偏宏观。

当前阶段新增了更具体的失败路径分析：

### 10.1 放松 guard 提圈速失败

扫描结果表明：

- `control_refine3_balanced2` 还能稳定出圈
- `speedup_a / b / c / d` 全部 `lap_sum = 0`

结论是：

- 当前不能继续靠“放松 guard”提升圈速

### 10.2 直接 refine4 硬训失败

对 `cth_v1_gtrack1_refine4_laptime_cpu` 的实验表明：

- 没有超过 `58.27s`
- 后续 interval eval 退化到约 `1071m`

结论是：

- 当前继续直接硬训，会把已经学到的稳定单圈重新训坏

这比旧版报告对失败实验的解释更细、更贴近当前研究阶段。

---

## 11. 对 `no_progress` 的理解更加深入

旧版报告中，`no_progress` 更多被当作一种终止现象来统计。

当前阶段则已经对其进行了行为层面的复盘，得到新的理解：

- 第一圈其实已经完成
- 完圈后车辆还能继续高速推进一小段
- 随后姿态开始失控
- 速度从 `170+` 快速下掉
- 朝向逐步转成接近反向
- 最后在低速或接近静止状态下长时间磨停
- 最终触发 `no_progress`

因此当前版本新增的认识是：

- 当前瓶颈不再只是“急弯减速不够”
- 而是“**过圈后继续推进与姿态保持能力不足**”

这也是为什么当前又新增了 post-lap reward 项的原因。

---

## 12. 如果把当前内容并回旧实验报告，建议新增哪些章节

如果要在旧版实验报告基础上升级成当前版本，建议至少新增以下内容：

### 12.1 新增“奖励函数升级”章节

需要补充：

- 为什么原始 baseline reward 已不足以支撑稳定单圈
- CTH reward 的组成项
- post-lap reward 的设计动机

### 12.2 新增“preview guard 设计与扫描”章节

需要补充：

- 预瞄净空与预瞄方向的定义
- guard 如何影响油门、刹车和转向
- `balanced_2` 为什么是当前最优点

### 12.3 新增“单圈指标与 checkpoint 选择逻辑”章节

需要补充：

- 为什么引入 `lap_count_sum` 和 `best_lap_time`
- 为什么 `cth*` 默认改用 `pace_first`
- 为什么旧的稳定性指标已经不够

### 12.4 新增“CPU/GPU 差异与工程复现”章节

需要补充：

- 同 checkpoint 在 CPU/GPU 上轨迹差异的现象
- 当前阶段为什么必须优先使用 CPU

### 12.5 新增“后段失稳与 no_progress 复盘”章节

需要补充：

- 为什么模型已经能跑完一圈，仍然会最终 `no_progress`
- 为什么后续优化重点要放在圈后姿态和继续推进能力

---

## 13. 一句话总结本次改动

相较上一版实验报告，当前版本最大的新增改动不是“又调了一轮参数”，而是：

- 从 `baseline_tuned` 的原始奖励调参实验
- 升级为 `CTH reward + preview guard + 单圈完成/圈速评价`
- 并围绕 `g-track-1` 单圈稳定完成这一目标，建立了一套新的训练、筛选和分析方法

如果用一句更适合写进报告的话来概括，可以写成：

> 上一版实验的核心突破是“从频繁出界提升到可稳定在赛道内运行”；而当前阶段的核心突破则是“在修改奖励函数并引入预瞄保护机制后，首次实现了 `g-track-1` 上的稳定单圈完成，并开始将优化目标从生存稳定性推进到单圈圈速与圈后持续推进能力”。
